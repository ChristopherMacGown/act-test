name: ruff
description: Runs the ruff linter against the local codebase.
runs:
  using: composite
  steps:
   - shell: /bin/bash
     run: |
       pip install ruff
       ruff check --fix . || echo "FAILED=ruff" >> "$GITHUB_OUTPUT"
