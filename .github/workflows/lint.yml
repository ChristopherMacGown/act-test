on:
  workflow_call:
jobs:
  lint:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        linters:
          - name: ruff
    name: lint ${{ matrix.linters.name }}
    steps:
      - uses: actions/checkout@v3
      - id: lint
        uses: ./.github/actions/linters/${{ matrix.linters.name }}
      - run: /bin/false