on: 
  - push
jobs:
  test:
    uses: ./.github/workflows/test.yml
  lint:
    uses: ./.github/workflows/lint.yml
    secrets: inherit
  format:
    needs: [lint]
    if: ${{ failure('lint') }}
    uses: ./.github/workflows/format.yml